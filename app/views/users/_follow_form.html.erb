 <% unless current_user?(@users_show) %>
  <div id="follow_form">
  <% if current_user.following?(@users_show) %>
    <%= render 'unfollow' %>
  <% else %>
    <%= render 'follow' %>
  <% end %>
  </div>
<% end %>  